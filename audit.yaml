audit:  # Configurations for auditing
  random_seed: 2345  # Integer specifying the random seed
  attack_list:
    - attack: lira
      online: True
      num_shadow_models: 256
      training_data_fraction: 0.5
      vectorized: True

  output_dir: "./shadow_models"
  attack_type: "mia"
  data_modality: "image" #image, tabular

target:
  # Target model path
  module_path: "src/models/resnet18_model.py"
  model_class: "resnet" 
  target_folder: "./target"
  # Data paths
  data_path: "./data/cinic10.pkl"

shadow_model:

distillation_model:
