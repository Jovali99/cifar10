audit:  # Configurations for auditing
  random_seed: 2345  # Integer specifying the random seed
  attack_list:
    - attack: lira
      online: True
      num_shadow_models: 4
      training_data_fraction: 0.5
      vectorized: True

  output_dir: "./shadow_models"
  attack_type: "mia"
  data_modality: "image" #image, tabular

target:
  # Target model path
  module_path: "src/models/wideresnet28_model.py"
  model_class: "WideResNet" 
  target_folder: "./target"
  # Data paths
  data_path: "./data/cifar10.pkl"

shadow_model:

distillation_model:
